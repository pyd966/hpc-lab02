# Lab02 MoE 的向量化计算

## 优化 MoE 前向

先阅读了 `moe_ref.cpp` 的实现，之后不难发现，每个 token 的计算是完全独立的，因此我们可以直用 OpenMP 多线程计算。

试验了一下，开 16 核的时候可以在 S3 的参数配置下直接得到 13.3 的加速。

接下来，我打算跑一下 profiling，并且因为我们只有 4 个 case，所以我打算 case by case 进行针对性优化。

S1 & S2 区别并不大，并且它们相比之下更为简单。我先跑了一下 S1 的 profiling，发现主要是 core_bound（69.7），这说明计算资源不够用。这是符合预期的，因为在这个 case 下 N=1，我们之前的多线程没有任何作用。

所以我们应该使用向量化来优化。

因为 router, quantization 之类的不是瓶颈所在，所以我们不把重点放在这里，而是放在 gate/up/down 这三个矩阵乘向量中。

